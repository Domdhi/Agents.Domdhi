{
  "name": "domdhi-agents",
  "private": true,
  "license": "MIT",
  "type": "commonjs",
  "scripts": {
    "build": "echo \"No build step — template-only\"",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "publish:public": "node .claude/core/publish.js"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "@vitest/coverage-v8": "^2.0.0"
  }
}
