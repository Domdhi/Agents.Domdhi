<!-- @@template -->
# Deployment

> **operations/** answers *how do we ship & run it?* — fill this in (and add
> `observability.md`, `security.md`, `runbooks/*.md` as the project earns them);
> delete this note when you do. Note: secret handling lives in
> `operations/security.md`, not a top-level `security/` folder.

## Environments
- {dev / staging / prod — URLs, how each is provisioned}

## Release Process
- {how a change goes from merged to live; rollback procedure}

## Runtime
- {where it runs, scaling, key config / env vars}
