{
  "version": "4.86",
  "updated": "2026-06-15",
  "changelog": "Cycle-2: build-loop intelligence + pipeline hardening. NEW /route command — a scale-adaptive planning-depth router (the build-loop front door): it classifies a request by Complexity + architectural reach and routes to the right pipeline depth (Tier 0 trivial/bugfix → /do; Tier 1 feature → /todo→/run-todo; Tier 2 module → /create:module; Tier 3 epic/greenfield → /create:new-project or /onboard). Routing != gate-bypass — it only picks an entry point; every downstream hard gate stays in force, skipped phases are named, and the decision is logged. Persisted per-task Complexity score (1-10): /todo + /create:project-epics-todo now emit a Complexity field against one canonical rubric, /do Step 6b routes on it (<=5 Opus-direct, >=6 Sonnet-delegate; missing-field falls back to the signal table; --delegate overrides) and surfaces it at pick-time, /review:optimize-backlog gathers it. Imperative-register softening for Opus 4.8: emphasis-only CRITICAL/MUST across skills+commands softened to plain imperatives while named severity tiers and safety/irreversibility guards are preserved (reduces Opus 4.8 over-triggering). gradeDoc deterministic PRD quality gate (doc-drift.js) wired into /create:project-requirements + /review:check-readiness, plus correctness fixes (annotated all-Must MoSCoW, header-only Success-Criteria table). SHIP_CHECK_OK evidence-gated ship token consumed by /do + /run-todo. /interview now persists its decision summary back into the artifact. memory-lint cross-reference fix (file-path citations no longer mis-read; sentence-final broken refs still caught).\n\n--- v4.85 ---\nSkill project-additions marker: skill .md files can now carry a <!-- @@project-additions --> line; everything below it is adopter-owned and preserved under template-updater --merge (the template head above still refreshes), the same way agent Soul Zones survive sync. Closes the hole where any project-specific guidance added to a template skill was clobbered on the next sync — generalizing the one-off skills/brand-guidelines/** project-exception into a section-level mechanism for any skill. No marker (or no --merge) = plain overwrite, exactly as before. Commands that customize template skills (/review:specialize, /sweep, /review:evolve-skills) now write project additions below the marker.\n\n--- v4.84 ---\nsettings.json merge: statusLine is now template-owned (it's a pointer at the template-owned core/statusline.sh, like hooks — so the command line tracks the template instead of freezing at the adopter's scaffolded value). A project that genuinely wants a custom status line overrides it in settings.local.json, which the sync always skips. Template-owned settings keys are now hooks + plansDirectory + statusLine; project-owned are permissions + env + $schema + any unknown keys."
}
