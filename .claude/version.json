{
  "version": "4.60",
  "updated": "2026-06-06",
  "changelog": "Tertiary /evolve enhancement — master-index next-epic resolution. Empty `/todo` (no INPUT) now reads docs/TODO_{Project}.md, picks the next epic (prefers an in-progress epic, else the first not-done one in index order; if all done, says cycle complete → suggests /evolve), resolves it to its epic number, and hands off to the epic-number branch. Closes the lifecycle 'what now?' loop no command previously read the master index for: after /evolve Step 5.6 makes the lead epic runnable, subsequent epics are picked up automatically by a bare `/todo` — the loop self-continues without re-running /evolve. Implemented as an inference source in /todo's empty-INPUT chain (#2, after conversation context, before handoff/git fallbacks) rather than a new /next command, to avoid command-surface bloat. evolve.md Step 5.6 cross-references the loop. Markdown-only; gate: 1879 tests pass.\n\n--- v4.59 ---\n/evolve adopter-finding remediation (Domdhi.Crypto first real run). (1) Primary — /evolve now ends RUNNABLE, not just planned: new Step 5.6 materializes the lead dependency-root epic into an execution-ready TODO_epic*.md via /create:project-epics-todo + /todo (only the lead epic, just-in-time; the rest break down as reached). Default on; --no-todo opts out. The Step-7 Next: line now points at a TODO that actually exists instead of a /run-todo with no contract behind it. (2) Secondary — /todo is now epic-number aware: `/todo 12` (or epic-N/epicNN) reads Epic N's stories straight from _backlog.md as the brief instead of treating the number as literal task text; mirrors /create:project-epics-todo and is the unit Step 5.6 calls. (3) EV4 — /evolve Step 1 completeness gate now cross-checks the _backlog.md emoji Story-Index against the checkbox tally and flags disagreement (checkboxes stay authoritative; stale-emoji drift is surfaced, not silently trusted). (4) EV7 — doc-drift.js TODO_SKIP_DIRS now includes `_archive` (underscore) so a closed cycle's git-mv'd TODO_epic*.md under docs/todo/_archive/cycle-N/ is no longer false-flagged as a misplaced TODO; the prior set only had `.archive` (dot). Already shipped in v4.57: EV3 (memory store in evidence sweep) + EV5 (headless orchestration). Gate: 1879 tests pass."
}
