{
  "version": "4.92",
  "updated": "2026-06-21",
  "changelog": "ADR 0006 .output taxonomy + memory-store split: the curated JSON memory store moves to a TRACKED docs/.output/.memory/ (cross-machine sync over git) while the derived FTS5 index, inbox, and daily logs go to a gitignored docs/.output/.state/; the whole .output/ durable zone reorganizes into four concern groups (handoffs/ findings/ plans/ evolution/) with monthly-bucketed, run-stamped layout via the OUTPUT_PATHS registry + output-paths.js helper. Read paths no longer mutate the tracked JSON (usage_count routes to the .state/ index only). Plus guardrail tuning (narrowed git-reset nudge, build-artifact rm exemptions, path-class hit telemetry), agent/skill pre-flight grounding rules, and the C2/C3 fork resolutions.\n\n--- v4.91 ---\nPost domain-taxonomy migration cleanup: purge stale _*.md doc-naming fossils. CLAUDE.md no longer claims docs use an underscore prefix and routes planning docs to their domain folders (was the dead docs/_project-*.md path); skill-authoring describes the Doc-producer archetype with real asset to domain mappings; docs/README.md points at the domain folders + docs/.output/handoffs/; and doc-drift.js report text + comments now match the shipped domain-taxonomy behavior (the report string had it backwards, calling _project-* the canonical naming when it is the superseded legacy form).\n\n--- v4.90 ---\nTask working files now live in docs/.output/.state/work/{date}/{task}/ (no more work/scratch/ relocation); work/ holds only the durable plan. New core tools: branch-audit (npm run branch:audit — safe branch/worktree prune audit), adr-lint (npm run adr:lint — ADR consistency over docs/architecture/decisions/), memory-backup (npm run memory:backup — out-of-repo memory-store backup)."
}
