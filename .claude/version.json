{
  "version": "4.89",
  "updated": "2026-06-21",
  "changelog": "docs/ DOMAIN-TAXONOMY MIGRATION (BREAKING for adopters' docs/ layout) + memory single-sourcing + /retro promotion.\n\nHEADLINE — BREAKING for adopters: docs/ is re-homed from flat _project-*.md files + scattered folders into seven concern-domains (product/ architecture/ design/ engineering/ operations/ work/ reference/), per ADR architecture/decisions/0005-docs-domain-taxonomy. The shipped .claude/ template now reads/writes the new layout everywhere — scaffold.js, every /create:* command, all agents, doc-drift.js, and the navigation contract docs/CLAUDE.md. ADOPTER ACTION REQUIRED after this sync: re-home your own docs/ by running, from your repo root, `node .claude/core/migrate-docs-domains.js --move docs/ --apply` then `node .claude/core/migrate-docs-domains.js --refs docs/ --apply`. It is one tested, idempotent, self-verifying codemod (history-preserving git mv; --verify exits non-zero if any legacy token survives) — dry-run first by omitting --apply. If you customized your docs/ paths, review the --dry-run diff before applying.\n\nAlso in this release:\n- Memory single-sourcing: per-category cap default raised 50 to 100; the 0.8 near-limit threshold and ALL relevance-scoring weights (MEMORY_SCORING: FTS + JSON-fallback paths) collapsed onto constants.js as the single source — eliminating drifting duplicate magic numbers across memory-manager, memory-guard, memory-lint, and session-start-prime.\n- /review:retro promoted to a root command /retro (the old namespaced name remains only in as-of-date historical records).\n- pull-request.js: spawn az/gh safely on Windows and stop masking spawn failures.\n\n--- v4.88 ---\ntemplate-updater now honors skillExclude for AGENT frontmatter. Previously the agent merge copied the template's skills: list verbatim, so an adopter whose update-config.json excludes skills (e.g. Mfa.Hub: no Tailwind, consolidated process skills into dev-process) got agent frontmatter naming skills it doesn't ship — a SILENT broken preload (subagent skills: is eager full-body injection with no Read event, so the agent loads NOTHING for that skill and behaves as if it had the knowledge; skill-resolution.js flags it BROKEN_SKILL_REF). A new stripExcludedSkills post-pass in agent-merger.js drops every excluded-skill reference from BOTH the frontmatter skills: list AND the ## Skills body path bullets, in both the merge and fresh-install paths. This is the root-cause cure for the regression where syncing a skillExclude adopter clobbered its curated agent skills with the template's. Pairs with the 4.87 skill-resolution checker that catches the failure mode.\n\n--- v4.87 ---\nTwo enforcement checkers close silent-failure gaps in the agent tier — the doctrine layer, landing on top of the cycle-2 4.86 base. (1) operating-standard.js: the mission at the top of CLAUDE.md ('resolve it or don't report it') is now ENFORCED through every agent, not merely declared — acting agents must load the anchor skill (verification-before-completion) AND carry an \"## Operating Standard\" section; report-only agents (disallowedTools: Edit) carry the report-only translation; shadow is exempt. The checker is fleet-safe: it reads each repo's update-config.json operatingStandardSkill, so an adopter that consolidated/renamed the anchor (e.g. Mfa.Hub to dev-process) is never forced to ship a skill it deliberately does not have. Wired into /review:check-templates Step 2c. tools/sync-work.js propagates the doctrine to off-roster adopters (anchor-detect, install checker, patch every agent, verify; dry-run default). (2) skill-resolution.js: a subagent's frontmatter skills list is EAGER full-body injection at startup (Claude Code native — sub-agents.md \"Preload skills into subagents\"), entering context through the runtime with NO Read event — so a broken or disable-model-invocation skill ref loads nothing SILENTLY (no runtime error, no telemetry signal, the agent behaves as if it had the knowledge). The new checker flags BROKEN_SKILL_REF (no SKILL.md) and UNPRELOADABLE (disable-model-invocation), turning the eyeball-only 'BROKEN SKILL REF' instruction into a tool-detectable check (the F8 primitive). Wired into /review:check-templates Step 2d."
}
